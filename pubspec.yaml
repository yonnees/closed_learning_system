name: closed_learning_system
description: Closed Learning System App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_tts: 4.2.5
  shared_preferences: ^2.5.4


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/data/languages/english/words.json
    - assets/data/languages/arabic/words.json
    - assets/data/languages/spanish/words.json
